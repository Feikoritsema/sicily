export function render(container) {
  container.innerHTML = `
    <section class="view-empty">
      <h1>Info</h1>
      <p class="view-empty__hint">Practical info, Emergency card, and Events land here in build order §10 step 7 — <code>data/practical-info.json</code> and <code>data/events.json</code> are already migrated.</p>
    </section>
  `;
}
