export function render(container) {
  container.innerHTML = `
    <section class="view-empty">
      <h1>Explore</h1>
      <p class="view-empty__hint">Place cards (filter/search/vote) land here in build order §10 step 4 — <code>data/places.json</code> is already migrated and ready to read from.</p>
    </section>
  `;
}
