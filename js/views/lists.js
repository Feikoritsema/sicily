export function render(container) {
  container.innerHTML = `
    <section class="view-empty">
      <h1>Lists</h1>
      <p class="view-empty__hint">Personal packing checklist, shared group items, and the shopping list land here in build order §10 step 6.</p>
    </section>
  `;
}
